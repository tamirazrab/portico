import userLangKey from "@/feature/common/lang-keys/user.lang-key";

const userEn: typeof userLangKey = {
  deleteUser: "Delete user {{name}}",
  failure: {
    usernameExists: "The user with this username already exist!",
  },
};

export default userEn;
